on:
  push:
    branches:
      - docker

name: Deploy to AWS ECS

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    env:
      REGION: us-east-1
      CLUSTER_NAME: staging
      SERVICE_NAME: omnia-staging

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Configure credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ env.REGION }}

    - name: Login to ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v1

    - name: Build, tag, and push image to ECR
      id: build-image
      env:
        IMAGE_TAG: latest
        ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
      run: |
        docker build -t ${ECR_REGISTRY}/${SERVICE_NAME}:${IMAGE_TAG} .
        docker push ${ECR_REGISTRY}/${SERVICE_NAME}:${IMAGE_TAG}
        echo "::set-output name=image::${ECR_REGISTRY}/${SERVICE_NAME}:${IMAGE_TAG}"

    - name: Download Feed Task Definition
      run: |
        aws ecs describe-task-definition --task-definition ${SERVICE_NAME}-feed --query taskDefinition > task-definition-feed.json
 
    - name: Render Feed Task Definition
      id: render-feed
      uses: aws-actions/amazon-ecs-render-task-definition@v1
      with:
        task-definition: task-definition-feed.json
        container-name: ${{ env.SERVICE_NAME }}-feed
        image: ${{ steps.build-image.outputs.image }}

    - name: Deploy Feed Service
      uses: aws-actions/amazon-ecs-deploy-task-definition@v1
      with:
        task-definition: ${{ steps.render-feed.outputs.task-definition }}
        service: ${{ env.SERVICE_NAME }}-feed
        cluster: ${{ env.CLUSTER_NAME }}
        wait-for-service-stability: true