[Unit]
Description=Gofer Agent
Requires=network.target
After=network.target

[Service]
Environment="PATH=/home/$USER/.nix-profile/bin"
User=$USER
Group=$USER
ExecStart=$GOFER_PATH -v debug -c "$GOFER_CONFIG" agent
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
